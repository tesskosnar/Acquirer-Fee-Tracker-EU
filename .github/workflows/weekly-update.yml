name: Weekly acquirer fee update

on:
  schedule:
    # Každé pondělí v 05:00 UTC
    - cron: "0 5 * * 1"
  workflow_dispatch: {}   # umožní ruční "Run workflow" v záložce Actions

permissions:
  contents: write   # potřeba, aby workflow mohl commitnout aktualizovaná data zpět

concurrency:
  group: weekly-acquirer-fee-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout repozitáře
        uses: actions/checkout@v4

      - name: Stáhnout nejnovější stav main (kdyby mezitím přibyla ruční úprava dat)
        run: git pull origin main

      - name: Nastavit Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Nainstalovat závislosti
        run: pip install -r requirements.txt

      - name: Spustit týdenní aktualizaci (kurz ČNB + kontrola zdrojů + přepočet)
        run: python scraper/update.py

      - name: Prověřit všechny publikované nabídky proti oficiálním zdrojům
        run: python scraper/audit_all_offers.py --workers 12 --linked-page-limit 3 --timeout 15

      - name: Spustit testy (kontrola, že se nic nerozbilo)
        run: python -m pytest -q

      - name: Commitnout aktualizovaná data
        run: |
          git config user.name "acquirer-fee-tracker-bot"
          git config user.email "actions@users.noreply.github.com"
          git add docs/data
          if git diff --cached --quiet; then
            echo "Žádná změna dat, není co commitovat."
          else
            git commit -m "chore: týdenní aktualizace poplatků ($(date -u +%Y-%m-%d))"
            # Pojistka pro nešťastné načasování (ruční push těsně mezi naším
            # pullem na začátku a pushem teď): zkusit rebase + push, max 3x.
            pushed=false
            for i in 1 2 3; do
              if git pull --rebase origin main && git push; then
                echo "Push úspěšný."
                pushed=true
                break
              fi
              echo "Pokus $i selhal, zkouším znovu za 10s..."
              sleep 10
            done
            if [ "$pushed" != true ]; then
              echo "Publikace selhala i po třech pokusech."
              exit 1
            fi
          fi

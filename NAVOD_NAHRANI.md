# Nejkratší postup

1. GitHub → New repository → `Acquirer-Fee-Tracker-EU` → Public.
2. Add file → Upload files → přetáhni všechny soubory a složky ze ZIPu.
3. Commit changes.
4. Settings → Pages → Deploy from a branch → `main` + `/docs` → Save.
5. Actions → Weekly acquirer fee update → Run workflow.
